# Progress

## Working
- Renderização da lista de projetos via `_data/projects.json` em `project.html`.
- CSS principal carregado via `_includes/head.html`.

## Recently completed
- Cards de projetos com tamanho mais consistente (altura do card e imagem normalizadas).

## Known issues / notes
- `assets/css/index.*.css` parece ser um bundle pré-compilado; para ajustes rápidos, preferir `assets/css/custom.css`.

