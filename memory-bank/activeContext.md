# Active Context

## Current focus
- Normalizar o layout da lista de projetos em `project.html` para que todos os cards tenham o mesmo tamanho.

## Recent changes
- Carregar `assets/css/custom.css` após o CSS principal.
- Ajustar markup/CSS dos cards de projetos para altura consistente (imagem com altura fixa + `flex` no conteúdo).

## Next steps
- Validar visualmente no browser (mobile/desktop).
- Se necessário, ajustar altura da imagem (`.project-card__img`) e espaçamentos.

